import React from "react";
import { Card } from "../components/Card";

export const Questions = () => {
  return (
    <>
      <Card category={null} difficulty={null} question={null} options={null} />
    </>
  );
};
